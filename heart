
        # for i in range(0, 360):
        #     t = math.radians(i)
        #     x = 16 * math.sin(t) ** 3
        #     y = 13 * math.cos(t) - 5 * math.cos(2 * t) - 2 * math.cos(3 * t) - math.cos(4 * t)
        #     x *= 0.05
        #     y *= 0.05
        #     pos = self.center + Vector2(x * radius, -y * radius)  # Adjusting scaling and flipping y-axis
        #     self.vertices.append(pos)
